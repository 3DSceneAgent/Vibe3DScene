# Shared fallback for all modes (optional)
VITE_BACKEND_URL=http://localhost:8000

# Optional mode-specific overrides.
# These take precedence over VITE_BACKEND_URL.
VITE_BACKEND_URL_DEV=http://localhost:8000
VITE_BACKEND_URL_PROD=https://api.example.com
